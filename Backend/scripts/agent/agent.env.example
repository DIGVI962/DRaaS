# Agent Environment Variables
# Set this to the public IP or hostname of the agent machine.
AGENT_IP=XXX

# URL for the scheduler service. Change if running on a different host/port.
SCHEDULER_URL=http://XXX:5000
